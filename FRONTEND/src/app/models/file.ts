export interface FileModel {
    _id: string;
    owner: string;
    file_name: string;
    extension_type: string;
    size: string;
    folder: string;
    public: boolean;
    updatedAt: string;
}